{
  "name": "@gtd/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "gtd": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(no tests in apps/cli yet)\" && exit 0"
  },
  "dependencies": {
    "@gtd/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
